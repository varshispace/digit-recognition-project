# digit-recognition-project
Handwritten digit recognition is a fundamental problem in the field of computer vision and deep learning. This project presents a system for recognizing handwritten digits using a Convolutional Neural Network (CNN). The model is trained and tested using the MNIST dataset, which consists of grayscale images of digits ranging from 0 to 9.
